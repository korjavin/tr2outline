version: '3.8'

services:
  tr2outline:
    image: ghcr.io/korjavin/tr2outline:latest
    container_name: tr2outline
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=3000
      - ANARLOG_WEBHOOK_SECRET=${ANARLOG_WEBHOOK_SECRET}
      - OUTLINE_URL=${OUTLINE_URL}
      - OUTLINE_API_KEY=${OUTLINE_API_KEY}
      - OUTLINE_COLLECTION_ID=${OUTLINE_COLLECTION_ID}
    # If deploying with Traefik reverse proxy, uncomment below:
    # networks:
    #   - default
    #   - ${TRAEFIK_NETWORK_NAME:-traefik}
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.tr2outline.rule=Host(`${DOMAIN}`)"
    #   - "traefik.http.routers.tr2outline.entrypoints=websecure"
    #   - "traefik.http.routers.tr2outline.tls.certresolver=letsencrypt"
    #   - "traefik.http.services.tr2outline.loadbalancer.server.port=3000"

# networks:
#   traefik:
#     external: true
#     name: ${TRAEFIK_NETWORK_NAME:-traefik}
